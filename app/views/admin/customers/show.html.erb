<div class="container">
  <h3><span style="background-color: silver;">さんの会員詳細</span></h3>

    <div>
      <p>会員ID <%= @customer.id %></p>
      <p>氏名 <%= @customer.last_name + @customer.first_name %></p>
      <p>フリナガ <%= @customer.full_name_kana %></p>
      <p>郵便番号 <%= @customer.postal_code %></p>
      <p>住所 <%= @customer.address %></p>
      <p>電話番号 <%= @customer.telephone_number %></p>
      <p>メールアドレス <%= @customer.email %></p>
      <p>会員ステータス <% if @customer.is_deleted %>
        <strang><span style="color:grey;">退会</span></strong>
      <% else %>
        <span style="color:limegreen;">有効</span>
      <% end %></p>
      
        <%= link_to "編集する", edit_admin_customer_path(@customer.id),class: "btn btn-success" %>
        <%= link_to "注文履歴一覧を見る", admin_root_path(@customer.id),class: "btn btn-primary" %>
    </div>
</div>